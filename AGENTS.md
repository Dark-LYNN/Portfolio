# AI Agent Instructions for "Lynnux's Portfolio"

This repository contains the code for Lynnux's portfolio.

## Critical Directives for AI Coding Assistants

1. **OLD Portfolio:** Lynnux's old portfolio was just a next.js app template this still has some good info about lynnux. see `/old`
2. **NEW Portfolio:** Lynnux's new portfolio is our project and goal, we will make a custom special portfolio in `/new`.

## Main goal

The main goal is to have `/new` be a working vite and react website. This website would have a `/interactive` endpoint which would be a working desktop environment which looks like a one to one of Lynnux's personal Linux with nixos rice. The current rice can be located at `/home/lynnux/.config` the rice we need to recreate is a NixOS, Hyprland with waybar. the hyprland and waybar configs can be found in `/home/lynnux/.config/hypr` and `/home/lynnux/.config/waybar`

for now we only  make the `/interactive` environment the main site just stays with the default page.tsx for now and so on